# 图像去噪实时演示项目 PPT 展示大纲

## 使用说明

这份大纲交给 PPT 制作成员使用。PPT 应围绕“上传图像后的实时计算演示”展开，不要做成预处理图片展示。建议总时长 8-10 分钟，其中原理 4-5 分钟，程序演示 3-4 分钟，总结 1 分钟。

建议风格：科技感、清晰、克制。主色可用深青色、白色、浅灰色，少量橙色强调噪声或频谱重点。每页尽量保留一张核心图或一组对比图，不要堆满公式。

---

## 第 1 页：标题页

**标题**：含噪图像去噪与清晰图像恢复实时演示系统

**副标题**：空域滤波、频域滤波、保边滤波与非局部均值方法对比

**页面内容**：
- 课程名称：《信号与系统》
- 小组成员与分工
- 展示日期

**画面建议**：
- 背景使用一张“含噪图像 → 去噪图像”的左右对比图。
- 标题放在上方或左侧，保持简洁。

**讲解要点**：
- 本项目不是展示提前处理好的图片，而是上传任意图片后实时计算。
- 目标是把信号处理中的空域、频域思想用于图像去噪，并用可视化方式比较效果。

---

## 第 2 页：项目背景与问题定义

**核心信息**：图像噪声会破坏视觉质量和后续识别，去噪需要在“去除噪声”和“保留细节”之间平衡。

**页面内容**：
- 图像噪声来源：传感器噪声、传输干扰、压缩损伤、老照片退化。
- 常见噪声类型：
  - 高斯噪声：整体随机扰动。
  - 椒盐噪声：随机黑白脉冲点。
  - 斑点噪声：乘性噪声。
  - 周期噪声：频域中常呈现集中分布。
  - 混合噪声：真实场景中更常见。
- 核心矛盾：平滑越强，噪声越少，但边缘和纹理也越容易损失。

**画面建议**：
- 用一张原图分别叠加几类噪声的小图矩阵。
- 可以用简单箭头标出“噪声抑制”和“细节保留”的拉扯关系。

**讲解要点**：
- 不同噪声的分布特点不同，所以不能只靠一种滤波器解决所有问题。
- 后续展示将围绕同一张上传图像实时生成噪声并比较方法。

---

## 第 3 页：项目目标与任务对应关系

**核心信息**：系统覆盖基础、进阶、挑战三个层级的课程要求。

**页面内容**：
- 基础目标：
  - 对不同噪声图像实现均值滤波、中值滤波。
  - 观察并对比空域滤波效果。
- 频域目标：
  - 对含噪图像进行 DFT 频谱分析。
  - 设计低通和带阻滤波器进行去噪。
  - 与空域方法进行对比。
- 进阶目标：
  - 实现双边滤波，分析参数对“去噪”和“保边”的影响。
- 挑战目标：
  - 实现非局部均值 NLM。
  - 支持混合噪声和真实受损图像的无参考观察。
- 工程目标：
  - 构建可演示、美观、易用的交互程序。

**画面建议**：
- 用三层阶梯图表示“基础 → 进阶 → 挑战”。
- 右侧放系统功能截图或功能结构图。

**讲解要点**：
- 每个算法不是孤立实现，而是放在统一界面中实时比较。
- 有参考图时使用指标，无参考图时用频谱、直方图和边缘图辅助观察。

---

## 第 4 页：系统整体流程

**核心信息**：用户上传图像后，系统现场完成加噪、去噪、频谱分析和指标计算。

**页面内容**：
1. 上传图像，作为干净参考图或已含噪图。
2. 选择噪声场景：高斯、椒盐、斑点、周期、混合或已含噪。
3. 调整算法参数。
4. 实时运行去噪算法。
5. 输出图像对比、频谱、滤波响应、直方图、边缘图和指标。

**画面建议**：
- 放一张从左到右的流程图：
  上传图像 → 噪声场景 → 滤波算法 → 可视化对比 → 结论分析

**讲解要点**：
- 强调 Demo Preset 只是自动填参数和触发实时计算，不是加载预处理结果。
- 上传不同图片后，所有输出都会重新计算。

---

## 第 5 页：程序界面与交互设计

**核心信息**：界面服务于课堂展示，既能快速演示，也能手动调参。

**页面内容**：
- 左侧参数栏：
  - 图像上传。
  - 演示预设。
  - 噪声场景。
  - 空域、频域、保边和 NLM 参数。
- 主区域：
  - 原图、含噪图、频谱图。
  - 各方法去噪结果。
  - 指标表格。
  - 频域响应、直方图、边缘图。
- 一键演示矩阵：
  - 高斯噪声、椒盐噪声、混合噪声多组并排比较。

**画面建议**：
- 使用程序截图，并用编号标注主要区域。
- 页面不要放太多文字，保留截图可读性。

**讲解要点**：
- 噪声参数会根据当前噪声类型动态显示，减少误操作。
- 对真实已含噪图，系统不强行显示 PSNR / SSIM。

---

## 第 6 页：噪声模型设计

**核心信息**：系统通过不同噪声模型模拟典型退化情况，并支持真实含噪输入。

**页面内容**：
- 高斯噪声：
  - 用标准差控制扰动强度。
  - 常用于模拟传感器随机噪声。
- 椒盐噪声：
  - 用噪声比例控制黑白脉冲点数量。
  - 中值滤波通常表现较好。
- 斑点噪声：
  - 乘性噪声，常见于某些成像系统。
- 周期噪声：
  - 在频谱中更容易观察到集中或带状特征。
  - 适合用带阻滤波器展示频域去噪思想。
- 混合噪声：
  - 更接近复杂真实场景。

**画面建议**：
- 一张 2×3 图像矩阵：原图、高斯、椒盐、斑点、周期、混合。
- 每张图下方放一句特征描述。

**讲解要点**：
- 噪声模型是为了对比算法特性，不代表所有真实噪声都能被单一模型完全描述。
- 真实老照片可以使用“已含噪图”模式做无参考修复观察。

---

## 第 7 页：空域滤波方法

**核心信息**：空域滤波直接在像素邻域上操作，简单直观，但容易损伤细节。

**页面内容**：
- 均值滤波：
  - 用邻域平均值替换中心像素。
  - 对高斯噪声有一定平滑效果。
  - 缺点：边缘容易模糊。
- 中值滤波：
  - 用邻域中位数替换中心像素。
  - 对椒盐噪声特别有效。
  - 缺点：对高斯噪声的细腻纹理保留不一定最好。
- 核大小影响：
  - 核越大，平滑越强。
  - 核过大，边缘和纹理损失明显。

**画面建议**：
- 左侧放均值滤波示意网格，右侧放中值滤波示意网格。
- 下方放高斯噪声和椒盐噪声的实际对比结果。

**讲解要点**：
- 空域滤波适合作为基线方法。
- 它能说明“去噪”和“模糊”之间的基本矛盾。

---

## 第 8 页：频域分析与 DFT

**核心信息**：DFT 将图像从空间分布转换到频率分布，让噪声特性更容易观察。

**页面内容**：
- 图像中的低频：
  - 对应大面积平滑变化和主体轮廓。
- 图像中的高频：
  - 对应边缘、纹理和部分噪声。
- DFT 幅度谱：
  - 中心附近通常代表低频。
  - 远离中心代表高频。
  - 周期噪声可能在频谱中形成明显亮点或带状结构。

**画面建议**：
- 放一组“含噪图像 + DFT 幅度谱”。
- 用圆形或标注指出低频区、高频区、周期噪声集中区。

**讲解要点**：
- 频域方法的优势是可以针对频率分布设计滤波器。
- 但频域低通会同时抑制噪声和图像细节，需要谨慎选参数。

---

## 第 9 页：频域滤波器设计

**核心信息**：系统实现多种二维径向频域滤波器，比较不同频率响应对去噪结果的影响。

**页面内容**：
- 低通滤波器：
  - 理想低通：截止边界最硬，可能带来振铃。
  - 高斯低通：过渡平滑，视觉效果较稳定。
  - 巴特沃斯低通：可通过阶数控制过渡陡峭程度。
  - 切比雪夫 I 型：通带存在纹波，过渡更陡。
  - 切比雪夫 II 型：阻带存在纹波，可增强阻带抑制。
  - 椭圆滤波器：通带和阻带都有纹波，过渡最陡但参数更敏感。
- 带阻滤波器：
  - 用于抑制特定频率带或周期噪声分量。
  - 可调中心半径、宽度和抑制强度。

**画面建议**：
- 使用滤波响应热力图或曲线对比。
- 可以用表格列出滤波器“过渡陡峭程度、参数敏感性、适用场景”。

**讲解要点**：
- 本项目中的切比雪夫和椭圆滤波器采用二维径向频率响应，用于图像频谱。
- 这些滤波器不是简单套用音频滤波，而是围绕图像 DFT 中心半径构造响应。

---

## 第 10 页：保边滤波：双边滤波

**核心信息**：双边滤波同时考虑空间距离和像素相似度，因此能在平滑噪声时尽量保留边缘。

**页面内容**：
- 双边滤波权重由两部分组成：
  - 空间距离：越近影响越大。
  - 像素差异：颜色或灰度越相近影响越大。
- 参数影响：
  - 空间 sigma 增大：影响范围扩大，平滑增强。
  - 颜色 sigma 增大：跨边缘混合增强，边缘更容易被模糊。
  - 颜色 sigma 较小：边缘保留更好，但去噪可能不足。

**画面建议**：
- 放“含噪图、均值滤波、双边滤波”的三图对比。
- 用局部放大框展示边缘处差异。

**讲解要点**：
- 双边滤波体现了进阶目标：不只是去噪，还要保留边缘。
- 参数选择本质上是在去噪强度和边缘保留之间折中。

---

## 第 11 页：挑战方法：非局部均值 NLM

**核心信息**：NLM 不只看局部邻域，而是在更大搜索范围内寻找相似图像块进行加权平均。

**页面内容**：
- 基本思想：
  - 图像中相似纹理可能出现在不同位置。
  - 用相似 patch 的加权平均恢复当前像素。
- 参数影响：
  - patch 大小：决定比较的局部结构尺度。
  - 搜索半径：决定寻找相似块的范围。
  - 强度系数：决定平滑力度。
- 优点：
  - 对混合噪声和纹理区域可能更稳定。
- 代价：
  - 计算量高于均值、中值和普通频域滤波。

**画面建议**：
- 用示意图表示一个 patch 在图像中寻找相似 patch。
- 放混合噪声下 NLM 和其他方法的对比。

**讲解要点**：
- NLM 是挑战部分，用来展示更高级的去噪思路。
- 它适合说明“利用图像自身冗余信息进行恢复”。

---

## 第 12 页：评价指标与无参考观察

**核心信息**：有干净参考图时使用定量指标；真实受损图像没有参考图时不能伪造指标。

**页面内容**：
- 有参考图：
  - MSE：均方误差，越小越好。
  - PSNR：峰值信噪比，越大越好。
  - SSIM：结构相似性，越接近 1 越好。
- 无参考图：
  - 观察频谱变化。
  - 观察灰度直方图。
  - 观察边缘图。
  - 结合主观视觉质量判断。

**画面建议**：
- 左侧放指标表格截图。
- 右侧放频谱、直方图、边缘图三个小图。

**讲解要点**：
- 指标不是越多越好，关键是使用条件正确。
- 对真实老照片，如果没有干净原图，PSNR 和 SSIM 没有可靠意义。

---

## 第 13 页：实验对比一：高斯噪声

**核心信息**：高斯噪声适合观察平滑类方法与保边方法的差异。

**页面内容**：
- 演示流程：
  - 上传一张边缘清晰的图片。
  - 选择“高斯噪声演示”。
  - 比较均值滤波、频域低通、双边滤波、NLM。
- 预期现象：
  - 均值滤波能降噪，但边缘变软。
  - 频域低通能压制高频噪声，但截止频率过低会损失细节。
  - 双边滤波边缘保留较好。
  - NLM 在纹理和结构保留上通常更稳。

**画面建议**：
- 用程序截图展示同一张图的四种结果。
- 指标表格可以标出最佳 PSNR 或 SSIM。

**讲解要点**：
- 不同指标和视觉结果可能不完全一致，需要结合分析。
- 参数调节时可以现场改变高斯标准差或低通截止频率。

---

## 第 14 页：实验对比二：椒盐噪声

**核心信息**：椒盐噪声是中值滤波的典型优势场景。

**页面内容**：
- 演示流程：
  - 选择“椒盐噪声演示”。
  - 比较均值滤波、中值滤波、双边滤波、频域滤波。
- 预期现象：
  - 均值滤波会把黑白脉冲扩散成灰色污染。
  - 中值滤波能有效去掉孤立黑白点。
  - 双边滤波对脉冲噪声不一定比中值滤波更优。
  - 频域低通不是椒盐噪声的最直接处理方式。

**画面建议**：
- 放一张椒盐噪声局部放大图。
- 用红框标出中值滤波后脉冲点减少的区域。

**讲解要点**：
- 这里可以说明“算法必须匹配噪声类型”。
- 同一参数不能适配所有场景。

---

## 第 15 页：实验对比三：周期噪声与带阻滤波器

**核心信息**：周期噪声在频谱中更容易被定位，带阻滤波器适合展示频域定向抑制。

**页面内容**：
- 演示流程：
  - 选择“周期噪声与带阻”。
  - 观察含噪图频谱。
  - 调整带阻中心半径、宽度、抑制强度。
  - 对比带阻滤波前后图像。
- 预期现象：
  - 周期干扰会在频谱中形成明显结构。
  - 带阻滤波器抑制特定频率后，周期纹理减弱。
  - 带宽过大可能损伤真实图像细节。

**画面建议**：
- 放“含噪图频谱 + 带阻响应 + 去噪结果”的三联图。
- 在频谱图上圈出被抑制的频率区域。

**讲解要点**：
- 带阻滤波器不是盲目平滑，而是针对频率分布做选择性抑制。
- 这页最能体现《信号与系统》中频域分析的价值。

---

## 第 16 页：参数选择与效果讨论

**核心信息**：去噪参数没有唯一最优值，应根据噪声类型、细节要求和评价方式综合选择。

**页面内容**：
- 空域核大小：
  - 小核保细节，去噪弱。
  - 大核去噪强，模糊明显。
- 频域截止频率：
  - 截止频率高，细节保留多，噪声残留多。
  - 截止频率低，噪声抑制强，细节损失多。
- 切比雪夫和椭圆滤波参数：
  - 阶数越高，过渡越陡。
  - 纹波和阻带衰减会影响视觉稳定性。
- 双边滤波：
  - 颜色 sigma 影响跨边缘平滑。
  - 空间 sigma 影响邻域范围。
- NLM：
  - 搜索范围和 patch 大小提升效果但增加计算成本。

**画面建议**：
- 用一张“去噪强度 vs 边缘保留”的二维坐标图。
- 在图上标注均值、中值、频域、双边、NLM 的大致位置。

**讲解要点**：
- 参数调节是本项目的重要展示点。
- 答辩时可以现场移动一个参数，让观众看到效果变化。

---

## 第 17 页：系统实现结构

**核心信息**：项目将算法核心和交互界面分离，便于维护和扩展。

**页面内容**：
- `app.py`：
  - Streamlit 界面。
  - 图像上传、参数控件、结果展示。
  - 一键演示矩阵。
- `denoise_core.py`：
  - 噪声生成。
  - 空域滤波。
  - 频域响应和 DFT 滤波。
  - 双边滤波和 NLM。
  - 频谱、边缘、直方图和指标。
- `CONTEXT.md`：
  - 项目术语和展示边界。
- `README.md`：
  - 运行方式和展示建议。

**画面建议**：
- 用模块结构图展示文件关系。
- 不要贴大段代码，只放函数名或模块职责。

**讲解要点**：
- 算法和界面分离使得后续新增滤波器或修改界面更方便。
- 项目使用 Python、Streamlit、NumPy、SciPy、scikit-image、Matplotlib。

---

## 第 18 页：现场演示脚本

**核心信息**：演示按固定节奏进行，避免现场操作混乱。

**页面内容**：
1. 打开程序并上传图片。
2. 选择“高斯噪声演示”：
   - 展示实时加噪、频谱和指标。
   - 调整低通截止频率或双边参数。
3. 选择“椒盐噪声演示”：
   - 说明中值滤波优势。
4. 选择“周期噪声与带阻”：
   - 展示 DFT 频谱和带阻滤波器响应。
5. 打开“一键演示矩阵”：
   - 快速总结不同噪声下的算法表现。

**画面建议**：
- 这页可作为演示前的“操作提词页”，不一定正式讲很多。
- 可以放一个二维码或本地运行命令截图。

**讲解要点**：
- 现场演示时尽量只改 1-2 个参数，避免时间失控。
- 强调所有结果基于当前上传图像实时计算。

---

## 第 19 页：项目结论

**核心信息**：不同噪声和图像结构需要不同去噪策略，频域分析和保边思想能显著提升解释性。

**页面内容**：
- 均值滤波：
  - 简单有效，但容易模糊。
- 中值滤波：
  - 适合椒盐噪声。
- 频域滤波：
  - 适合展示噪声频谱分布和滤波器设计。
  - 带阻滤波器适合周期噪声。
- 双边滤波：
  - 在去噪和保边之间取得较好平衡。
- NLM：
  - 对复杂噪声有更强表现，但计算成本更高。
- 系统价值：
  - 将理论、算法和实时可视化结合，便于课堂答辩展示。

**画面建议**：
- 用一张总结表格对比五类方法。
- 表格列：适合噪声、优点、缺点、是否保边、计算成本。

**讲解要点**：
- 没有单一算法对所有图像和所有噪声都最优。
- 本项目的重点是“看得见、能交互、能比较”的信号处理演示。

---

## 第 20 页：展望与可扩展方向

**核心信息**：系统还能继续扩展到真实图像修复和更高级算法。

**页面内容**：
- 真实老照片修复：
  - 划痕、褪色、局部缺损。
  - 可结合无参考质量评价和局部修复算法。
- 更高级算法：
  - 小波阈值去噪。
  - BM3D。
  - 深度学习去噪网络。
- 更完整实验：
  - 批量测试不同噪声强度。
  - 导出结果报告。
  - 增加运行时间比较。

**画面建议**：
- 用路线图表示“当前系统 → 传统高级算法 → 深度学习修复”。

**讲解要点**：
- 当前项目已经满足课程展示的核心要求。
- 后续方向可以作为答辩时回答“还能怎么改进”的准备。

---

## 附录页 A：方法对比表

| 方法 | 主要思想 | 适合场景 | 优点 | 局限 |
| --- | --- | --- | --- | --- |
| 均值滤波 | 邻域平均 | 轻度高斯噪声 | 简单、速度快 | 容易模糊边缘 |
| 中值滤波 | 邻域中位数 | 椒盐噪声 | 去除脉冲点效果好 | 对纹理和高斯噪声不一定最佳 |
| 频域低通 | 抑制高频成分 | 高频噪声 | 频域解释性强 | 可能损失边缘和纹理 |
| 带阻滤波器 | 抑制特定频带 | 周期噪声 | 针对性强 | 参数选择依赖频谱观察 |
| 双边滤波 | 空间距离 + 像素相似度 | 需要保边的平滑 | 保边效果较好 | 参数敏感 |
| NLM | 相似 patch 加权平均 | 混合噪声、纹理图像 | 利用图像冗余信息 | 计算成本较高 |

---

## 附录页 B：答辩可能问题与回答

**问题 1：为什么需要频域分析？**

回答：空域只能直接观察像素邻域，而 DFT 可以看到噪声在频率上的分布。周期噪声这类问题在频谱中更明显，因此适合用带阻滤波器定向抑制。

**问题 2：为什么椒盐噪声中中值滤波更好？**

回答：椒盐噪声是孤立的极大或极小值，中位数对这种异常值不敏感，因此能去掉脉冲点，同时比均值滤波更不容易把黑白点扩散成灰色污染。

**问题 3：为什么双边滤波能保边？**

回答：双边滤波不仅看空间距离，还看像素值相似度。边缘两侧像素差异较大，权重会降低，所以不会像均值滤波那样直接跨边缘平均。

**问题 4：为什么真实受损图不显示 PSNR / SSIM？**

回答：PSNR 和 SSIM 需要干净参考图。真实受损图通常没有对应的干净原图，强行计算会误导，所以系统改用频谱、直方图、边缘图和视觉效果进行无参考观察。

**问题 5：切比雪夫和椭圆滤波器在图像里怎么用？**

回答：项目中将这些滤波器作为二维径向频域响应使用。图像 DFT 后以频谱中心为原点，根据半径计算响应，再作用到图像频谱上，从而实现图像频域低通。

---

## PPT 制作注意事项

- 全文统一使用“带阻滤波器”，不要写“陷波滤波器”。
- 统一强调“实时计算”，不要暗示结果来自预处理图片。
- 截图时优先选择边缘清晰、纹理丰富的图像。
- 每个算法页只放关键公式或示意图，避免大段推导。
- 演示页应保留操作路径，方便主讲人按步骤讲。
- 对比图片要使用同一张上传图像，确保公平比较。
